from unittest.mock import Mock

import pytest


# @pytest.fixture
# def send_task_mock(monkeypatch):
#     send_task_mock = Mock()
#     monkeypatch.setattr('send_task', send_task_mock)
#     return send_task_mock
